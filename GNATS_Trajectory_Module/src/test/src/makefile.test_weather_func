CXX=g++

EXECUTABLE=test_weather_func

SOURCES=$(shell find . -name 'test_weather_func*.cpp')
HEADERS=$(shell find . -name 'test_weather_func*.h')

OBJS=$(SOURCES:.cpp=.o)

CXXFLAGS=-g -O3 -fPIC -Wall -Wextra
LDFLAGS=-L/usr/local/lib -L../lib -L../../../lib -L../../wind_tool/third-party/hdf5install/lib
LIBS=-ljson-c -lastar -lrg -ltg -lcommon -lhuman_error -lcontroller -lcuda_compat -lpilot -ltrx -lbada -lfacet_data -llektor -lfp -lgeomutils -lm -lghthash -lhdf5 -lz -ldl -lgomp -lairport_layout -lwind
INCLUDE_DIRS= \
	-I../include \
	-I../../../include/libairport_layout \
	-I../../../include/libcommon \
	-I../../../include/libcuda_compat \
	-I../../../include/libfacet_data \
	-I../../../include/libbada \
	-I../../../include/libhuman_error \
	-I../../../include/libtrx \
	-I../../../include/libgeomutils \
	-I../../../include/libtg \
	-I../../../include/libfp \
	-I../../libairport_layout/src \
	-I../../libcuda_compat/src \
	-I../../libfacet_data/src \
	-I../../libbada/src \
	-I../../libtrx/src \
	-I../../libgeomutils/src \
	-I../../libtg/src \
	-I../../libfp/src \
	-I../../wind_tool/third-party/hdf5install/include

all:
#	$(CXX) $(CXXFLAGS) -x c++ $(CPPFLAGS) $(INCLUDE_DIRS) -c $(SOURCES) -o $(OBJS)
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) $(INCLUDE_DIRS) -c $(SOURCES)
	@echo ""
	@echo "Generating shared library file...."
	@echo ""
#	$(CXX) $(CXXFLAGS) $(LDFLAGS) $(NEWFUNC_OBJS) -shared -o libNewFunc.so $(LIBS)
	@echo ""
	@echo "Generating executable file...."
	@echo ""
	$(CXX) $(CXXFLAGS) $(LDFLAGS) $(EXECUTABLE).o -o $(EXECUTABLE) $(LIBS)

clean:
	rm -rf *.o $(EXECUTABLE)